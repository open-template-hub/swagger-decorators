 '{{RESPONSE_CODE}}': {
  description: '{{RESPONSE_DESCRIPTION}}',
  content: {
    'application/json': {
      schema: {
        type: 'array',
        items: {
          $ref: '#/components/schemas/{{RESPONSE_SCHEMA}}'
        },
      },
    },
  },
},